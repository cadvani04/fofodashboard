{
  "name": "fofo-dashboard",
  "version": "1.0.0",
  "description": "Full-stack dashboard application with FastAPI backend and Next.js frontend",
  "main": "start.sh",
  "scripts": {
    "start": "./start.sh",
    "build": "./start.sh",
    "dev": "concurrently \"cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000\" \"cd webapp && npm run dev\"",
    "install-deps": "cd backend && pip install -r requirements.txt && cd ../webapp && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.11.0"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "."
  },
  "keywords": ["dashboard", "fastapi", "nextjs", "fullstack"],
  "author": "",
  "license": "MIT"
}
